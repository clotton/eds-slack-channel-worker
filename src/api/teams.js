import { jsonResponse } from '../utils/response.js';

// Placeholder for Teams support

export async function handleTeamsExample() {
    // Implement when Microsoft Teams support is added
    return jsonResponse({ message: "Teams integration coming soon!" });
}
